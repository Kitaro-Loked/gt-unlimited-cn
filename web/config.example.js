/*
 * GT UNLIMITED — optional authentication config
 *
 * 1. Copy this file to web/config.js
 * 2. Fill in your own credentials
 * 3. Add this line BEFORE <script src="/assets/app.js"></script> in index.html:
 *    <script src="/config.js"></script>
 *
 * If config.js is not loaded, the terminal boots without login (default).
 */
window.TERMINAL_AUTH = {
  username: 'admin',
  password: 'change-me',
};
